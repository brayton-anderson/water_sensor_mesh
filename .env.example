# Absolute path to the local water_sensor_mesh config.yaml file
CONFIG_PATH=/path/to/water_sensor_mesh/config.yaml
# USB device to passthrough (`lsusb -t`: look for `ch341`)
USB_DEVICE=/dev/bus/usb/001/037
